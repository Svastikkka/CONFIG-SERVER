---
- name: Ping all hosts
  hosts: "localhost"
  become: yes
  tasks:
    - name: Ping the hosts
      ping:

    - name: Copy updated configuration file
      copy:
        content: "{{ lookup('file', '{{ src_path }}') }}"
        dest: "/tmp/nginx.conf"